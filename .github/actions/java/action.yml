name: "Java setup"
description: "Setup Java"

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v6

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        distribution: "temurin"
        java-version: "21"
        cache: gradle
