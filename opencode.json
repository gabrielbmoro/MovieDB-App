{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "skills": {
    "paths": ["ai/skills"]
  },
  "permission": {
    "read": { "**/local.properties": "deny" },
    "grep": { "**/local.properties": "deny" },
    "glob": { "**/local.properties": "deny" }
  },
  "command": {
    "setup-hooks": {
      "template": "Run 'git config core.hooksPath .githooks' from the project root so git uses the .githooks/pre-commit script which runs detektAll on every commit.",
      "description": "Configure git to use .githooks/ for pre-commit hooks"
    }
  },
  "mcp": {
    "kotzilla": {
      "type": "remote",
      "url": "https://mcp.kotzilla.io/mcp",
      "enabled": true
    }
  }
}
